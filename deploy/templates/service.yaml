apiVersion: v1
kind: Service
metadata:
  name: {{ include "ilo4-metrics-proxy.fullname" . }}
  labels:
    {{- include "ilo4-metrics-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ilo4-metrics-proxy.selectorLabels" . | nindent 4 }}
